# Verify that we can open and read the election results CSV correctly# Showing a "test-driven" stylefrom electiondata import ElectionResults, ElectionResultsWrap, ElectionScrapperimport unittestclass ElectionResultsTest(unittest.TestCase):	def setUp(self):		self.results = ElectionResults('2012_US_election_state.csv')		self.results1 = ElectionResultsWrap('2012_US_election_state.csv')		self.scrapper = ElectionScrapper()		self.scrapper.load_url("http://www.archives.gov/federal-register/electoral-college/2012/popular-vote.html")		self.scrapper.load_filename("election_full_test_file.csv")	def testLoad(self):		self.results.load()		assert self.results!=None		assert self.results.file!=None	def testLoad1(self):		self.results1.load()		assert self.results1!=None		assert self.results1.file!=None				def testStateCount(self):	  self.results.load()	  state_count = self.results.state_count()	  assert state_count==44	def testStates(self):	  self.results.load()	  names = self.results.states()	  assert len(names)==44	  assert names[0]=='Alaska'	  assert names[1]=='Alabama'	def testCand(self):		self.results.load()		cand_count = self.results.candidates_count()		assert len(cand_count)==2	# Test Wrapper Class Capabilities	def testFile(self): 		self.results1.load()		assert self.results1.file != None	def get_fileData(self):		self.results1.load()		assert self.results1.all_lines != None		assert self.results1.all_lines[1].split(',')[1] == 'Alaska'	def get_states_vec(self):		self.results1.load()		assert self.results1.states_vec != None		assert self.results1.states_vec[0] =='Alaska'	def get_state_count(self):		self.results1.load()		assert self.results1.state_count == 44	# Test Scrapper Class Capabilities	def testScrapeURL(self):		self.scrapper.scrape_url()		assert self.scrapper.data != None		assert len(self.scrapper.data) == 50+1+1 #50 States+DC+header		assert self.scrapper.data[0][0].strip() == 'State'		assert self.scrapper.data[1][1].strip() == 'Alabama'		assert self.scrapper.data[-1][1].strip() == 'Wyoming'# if this file is run directly, run the testsif __name__ == "__main__":	unittest.main()